$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json

name: UMD Element Slider Events
props:
  type: object
  required:
  - headline
  properties:
    headline:
      type: string
      title: Headline
    theme:
      type: string
      title: Theme
      description: Use 'light', 'dark'
  slots:
    dates_slot:
      title: Dates
      description: Slot intended for Events Dates
    cta_slot:
      title: Call to Action slot
      description: Slot intended for CTA
