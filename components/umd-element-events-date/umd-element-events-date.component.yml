$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json

name: UMD Events Date (Broken)
group: UMD Design System
label: Date

props:
  type: object
  properties:
    event_date:
      type: string
      title: Date
    event:
      type: string
      title: Event Name
    event_url:
      type: string
      title: Event URL
    theme:
      type: string
      title: Theme
      description: Use 'light', 'dark'
