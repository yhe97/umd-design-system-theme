$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json

name: UMD CTA
props:
  type: object
  properties:
    cta:
      type: string
      title: Call to Action
      description: URL
    cta_text:
      type: string
      title: Call to Action text
      description: Text related to CTA
    cta_aria_label:
      type: string
      title: Call to Action aria-label
      description: Text related to CTA
    element_size:
      type: string
      title: Use 'standard'
      description: Text related to CTA
    theme:
      type: string
      title: Theme
      description: Use 'light', 'dark'
    action_type:
      type: string
      title: Action Type
      description: Use 'primary' or 'secondary'
