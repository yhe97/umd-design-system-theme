$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json

name: UMD CTA
group: UMD Design System
label: Call to Action

props:
  type: object
  properties:
    element_size:
      type: string
      title: Use 'standard'
      description: Text related to CTA
    theme:
      type: string
      title: Theme
      description: Use 'light', 'dark'
    display:
      type: string
      title: Display Type
      description: Use 'outline', 'secondary', 'primary'
slots:
  cta:
    title: CTA URL (String / URL)
    description: The Call to Action URL
  cta_text:
    title: CTA Text (String / Token)
    description: Text related to CTA
  cta_aria_label:
    title: CTA aria-label (String / Token)
    description: For accessability
